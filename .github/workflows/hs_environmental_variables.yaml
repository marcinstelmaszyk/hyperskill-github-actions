name: environmental_variable_tutorial
on: [push]
env: 
  TOPIC: Envs and secrets
jobs:
  job1:
    env: 
      section: Dev tools
    runs-on: ubuntu-latest
    steps:
      - name: step1
        env:
          platform: the platform
        run: | 
          echo "Github actor: $GITHUB_ACTOR $GITHUB_REPOSITORY $GITHUB_REF" 
    